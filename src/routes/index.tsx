import { BrowserRouter, Route, Routes } from "react-router-dom";

const AppRoutes = () => {
    <BrowserRouter>
        <Routes>
            <Route index />
        </Routes>
    </BrowserRouter>
}

export default AppRoutes;